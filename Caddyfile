www.{$DOMAIN} {
    redir https://{$DOMAIN}{uri} permanent
}

{$DOMAIN} {
    encode gzip zstd

    @api path /api/*
    handle @api {
        reverse_proxy backend:3333
    }

    handle {
        reverse_proxy frontend:80
    }
}
